# Optional. If set, the dev server caches Wikipedia/Commons responses
# in Neon Postgres for faster repeat loads. If absent, an in-memory
# cache is used and the rest of the app works identically.
DATABASE_URL=postgresql://user:password@ep-xxx.eu-central-1.aws.neon.tech/neondb?sslmode=require
